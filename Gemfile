source 'https://rubygems.org'

ruby '3.1.0'  # Specify the Ruby version you're using (adjust as needed)

# Sinatra for your web server
gem 'sinatra', '~> 3.0'

# Faye for WebSocket support
gem 'faye-websocket', '~> 0.10.0'

# Puma for better production performance (optional, but recommended)
gem 'puma', '~> 5.0'

# For handling JSON data
gem 'json', '~> 2.6'

# For running in a production environment and managing environment variables
gem 'dotenv', '~> 2.7'

# For development purposes, a gem to enable live reload
gem 'rerun', '~> 0.13'

# Optional: For background job processing if needed (e.g., ActiveJob or Sidekiq)
# gem 'sidekiq', '~> 6.0'
